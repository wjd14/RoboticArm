import sys;
import math;
import time;
from ax12 import Ax12;


class Mover:
    self = Ax12();

    def move(self, number, ticks, speed):
        self.moveSpeed(number, ticks, speed);
        return;

    def read(self, number)
